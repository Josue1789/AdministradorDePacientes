import React, { useState } from "react";

function Header({titulo}){

    return(
        <div className="Header">
            <h1>{titulo}</h1>
        </div>
        
    )
}

export default Header